# Copy to .env and adjust for local development:
#   cp .env.example .env

MICROSCOPE_DATABASE_URL=postgres://user:pass@localhost:5432/microscope?sslmode=disable
MICROSCOPE_ADDR=:8093
# URL path prefix (leading slash). make run disables Git Bash path conversion when loading .env.
MICROSCOPE_PATH=/microscope
